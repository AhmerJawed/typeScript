// Declare a variable to store the person's name
let personName: string;

// Assign a name to the variable
personName = "Eric";

// Construct the message using string interpolation
let message: string = `Hello ${personName}, would you like to learn some Python today?`;

// Print the message to the console
console.log(message);
